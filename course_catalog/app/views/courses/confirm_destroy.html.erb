<h1>Confirm Deletion</h1>

<p>Are you sure you want to delete the course <strong><%= @course.number%></strong>: <strong><%= @course.name%></strong>?</p>

<div>
  <%= form_with url: course_path(@course), method: :delete do %>
    <%= submit_tag 'Yes, Delete', class: "button" %>
  <% end %>
</div>

<div>
  <%= link_to 'Cancel', courses_path, class: "button" %>
</div>
